source "https://rubygems.org"

gem "jekyll", "~> 4.2.0"
gem "minima", "~> 2.5"
gem "jekyll-paginate", "~> 1.1.0"
gem "jekyll-sitemap" # Keep this single instance
gem "jekyll-seo-tag" # SEO tags
gem "jekyll-feed" # Keep this single instance
# gem "jekyll-google-fonts" # This gem doesn't exist

group :jekyll_plugins do
  gem "jekyll-feed" # Remove this duplicate entry if present
  gem "jekyll-sitemap" # Remove this duplicate entry if present
end
